# Work order — T-184 Ticket A: delete confinement, one run dir, ENVELOPE.md

You are the implementer for one bounded ticket on branch `feat/t-184-subtractive-delegation` in this worktree. Everything below is pre-aligned (approved plan doc-122, Cuts 1–4 plus their batch-2 folds); your job is execution. New consequential decisions or scope gaps: STOP and report in the envelope's `openQuestions`.

Read first: `backlog/docs/plans/doc-122 - Plan-—-Delete-delegate-confinement-and-rebuild-delegate-lifecycle-on-one-run-dir.md` (Cuts 1–4 and their batch-2 folds bind), `backlog/tasks/t-184 - Delete-delegate-confinement-and-rebuild-delegate-lifecycle-on-one-run-dir.md` (ACs 1, 2, 7, 9 bind this ticket), and `REVIEW.md`. Never edit `backlog/`; never push; never amend.

## Design (owner-settled; implement exactly)

**Before:** `bin/qq-dispatch` wraps each pi-subagents child in a Landstrip policy (roles.json + `bin/lib/qq-render-landstrip-policy.mjs`), validates a structured-output capture path, and mints the child's run dir itself. Parents pass `outputSchema`; children emit through a `structured_output` capture seam.

**After:** the git worktree is the only boundary. One durable run dir per delegate owns lifecycle state **from ticket creation**: the OWNER creates it, the brief lives in it at dispatch by construction, the child writes `ENVELOPE.md` there (the only result surface — no schemas passed, no capture seam), and the adapter writes a `TERMINAL` file there at child exit.

### qq-dispatch (rewrite; currently 469 lines)

KEEP (same behavior unless noted):
- Fail-closed posture, `umask 077`, `bin/lib/qq-bin.sh` resolution, git common-dir and invocation-cwd validation (refuse non-Git cwd, refuse cross-Repository dispatch), the T-177 global rule (canonical adapter serves any resolvable Git Repository; linked-worktree adapters serve their own common dir only).
- Role manifest resolution via `PI_SUBAGENT_CHILD_AGENT` + `PI_SUBAGENT_TRUSTED_AGENT_PATHS` (exact canonical manifest validation stays).
- Execution-profile resolution/receipt machinery (unchanged — it is not confinement).
- Auth staging into the staged pi config dir; `PI_CODING_AGENT_DIR` export; pi-subagents session-root config validation (lines ~290–330); `bin/lib/qq-process-tree-supervisor.py` bounded descendant cleanup; timeout handling (`QQ_DISPATCH_TIMEOUT`); the trace/telemetry spans (`qq-observe record` hooks); direct child environment exports (`QQ_DISPATCH_WORKTREE`, `QQ_DISPATCH_GIT_COMMON_DIR`, `QQ_DISPATCH_GIT_WORKTREE_DIR`, `QQ_DISPATCH_RUN_DIR`, `QQ_DISPATCH_POLICY_IDENTITY` — drop `POLICY_IDENTITY`, see below).

DELETE:
- Landstrip entirely: binary resolution (~lines 99–104, 252–273), policy render invocation (~332–353), `bin/lib/qq-render-landstrip-policy.mjs` (delete file), `delegation/policies/roles.json` (delete file), the Landstrip wrapper around the child launch (launch the child directly), `QQ_DISPATCH_POLICY_IDENTITY` export, and every Landstrip mention in comments.
- The structured-output capture validation (~lines 130–151) and every capture-seam reference. `PI_SUBAGENT_STRUCTURED_OUTPUT_CAPTURE` is ignored going forward.
- The confined `$TMPDIR` staging whose only purpose was policy-granted scratch. The child inherits a normal environment; scratch guidance is the skill's prose problem, not the adapter's.

CHANGE — the run-dir contract:
- New input: `QQ_DISPATCH_RUN_DIR` may be PRESENT in the adapter's environment at dispatch (the owner creates it). Validate fail-closed: absolute path; beneath the runtime root (`QQ_DISPATCH_RUNTIME_ROOT`, default unchanged); real directory, not a symlink; owned by euid; mode 700; contains no entries other than an optional `BRIEF.md`. When valid, use it as the child's run dir instead of minting one. When absent, mint one exactly as today (owner-less dispatch remains possible).
- At child exit (any status), the adapter writes `TERMINAL` in the run dir: one JSON line `{"schema":"qq-run-terminal","version":1,"run_id":...,"agent":...,"exit_code":N,"ended_at":ISO}` (atomic write via tmp+rename). This replaces receipts as the durable lifecycle record.
- New keep — role startup validation: before launch, parse the role manifest frontmatter `tools:` list and refuse dispatch (exit 66 with a clear message) when any declared tool is not in the adapter's known inventory. Inventory: the pi builtin tool names (read, bash, edit, write, grep, glob, find, ls) plus `subagent`, `subagent_wait`, `intercom`, `ask_user_question`, `web_search`, `web_fetch`, `agent_browser`, `todo`, `lsp_diagnostics`, `lens_diagnostics`, `symbol_search`, `module_report`, `read_symbol`, `read_enclosing`, `pi_lens_activate_tools`, `operator_stage`, `qq_pr_watch`, `architect_disposition`. Single static list in the adapter with a comment that it is the deliberate choke point; a manifest naming anything else fails closed.
- Reviewer/read-only cache redirect: for ALL roles, export `XDG_CACHE_HOME="$run_dir/cache"` (created mode 700) so tool caches land beneath the run dir.

### Manifests

- Delete `delegation/manifests/completion-envelope.schema.json`.
- New `delegation/manifests/ENVELOPE.md` — the completion-envelope template the delegate copies into its run dir and fills: sections Status (complete|blocked|failed), Summary, Commits, Checks (name + pass|fail|inconclusive-under-substrate + one-line evidence each), Files changed, Contestable decisions, Open questions, Unresolved risks, Branch, Worktree. One page, markdown, no schema language.
- All four `delegation/manifests/agents/*.md`: delete the `acceptanceRole:` frontmatter line (the writer-role lane is gone; `acceptance: {level: none, ...}` stays exactly as-is). Update implementer.md body: the delegate returns its result by writing `ENVELOPE.md` into `$QQ_DISPATCH_RUN_DIR` following `delegation/manifests/ENVELOPE.md` — no structured-output schema, and a delegate that ends on a user message without ENVELOPE.md is failed by construction. Same one-liner for reviewer/researcher/observer bodies adapted to their result (reviewer verdict, research report, analysis file path) — each writes ENVELOPE.md.

### extensions/qq-subagent-env.ts

- Keep every env var it sets (adapter path, agent dirs, trusted paths/profiles, runtime root, session-root hygiene).
- Rewrite the header comment: no longer "confined-delegate dispatch env" — it is the delegation dispatch env; the git worktree is the only boundary. Delete the structured-output capture/T-129 waiver comment block (lines ~10–16).

### Tests

- DELETE `tests/test-qq-delegate-enforcement.sh` (the failure class it enforced no longer exists).
- REWRITE `tests/test-qq-dispatch.sh` (currently 1319 lines; expect ~400–600): keep the still-valid cases (git/cwd refusals, T-177 global/canonical matrix, trusted-manifest validation, timeout precedence, auth staging refusal, session-root config refusal, telemetry spans, process-tree cleanup) and add the new contract: owner-created run dir accepted (valid) / refused (symlink, loose mode, foreign path, unexpected entries); adapter mints when absent; TERMINAL written on success and on failure exit; startup validation refuses a manifest declaring an unavailable tool; no Landstrip binary/policy is touched (fixture asserts `qq-render-landstrip-policy.mjs` and `roles.json` are gone and dispatch succeeds without a landstrip binary present); ENVELOPE.md is the only result surface (no structured-output capture path is consulted).
- UPDATE `tests/test-qq-subagent-env.sh` for the new extension comments/behavior (env vars unchanged).

## Constraints

- Smallest resulting system: this is a deletion-first ticket. No replacement parity machinery, no new flags beyond the run-dir input, no speculative options.
- Scratch beneath your normal `$TMPDIR`; never literal `/tmp` paths in committed artifacts (fixtures may create temp dirs at test runtime), never worktree-local scratch left behind.
- Do not touch: `bin/qq-observe`, `bin/qq-change`, `bin/qq-execution-profiles`, `skills/`, `README.md`, `CONCEPTS.md`, `REVIEW.md`, `AGENTS.md`, `backlog/`, `openwiki/`, `patches/`, `extensions/qq-execution-profiles.ts`, other extensions. (Owned by Tickets B/C/D or out of scope.)
- `tools/ratchet-baselines.conf` budgets are unaffected by this ticket (no scoped prose); if `tools/ratchet.sh check` surprises you, report, don't adjust.

## Commit protocol

Two commits on `feat/t-184-subtractive-delegation`, in order:
1. `delegate: delete Landstrip confinement and structured-output pipeline (T-184 cut 1-2)` — adapter rewrite, renderer/roles/schema deletions, ENVELOPE.md template, manifests, extension.
2. `tests: redispatch contract suite for worktree-only boundary (T-184 cut 3-4)` — test deletion/rewrite/update.
Never push.

## Checks (run each; report real output)

1. `bash tests/test-qq-dispatch.sh` — pass.
2. `bash tests/test-qq-subagent-env.sh` — pass.
3. `shellcheck -x bin/qq-dispatch tests/test-qq-dispatch.sh tests/test-qq-subagent-env.sh` — clean.
4. `bash -n bin/lib/qq-process-tree-supervisor.py 2>/dev/null; python3 -m py_compile bin/lib/qq-process-tree-supervisor.py` — clean (if touched; otherwise skip and say so).
5. `git grep -n "landstrip\|Landstrip" -- bin delegation extensions tests` — zero matches except the deliberate historical comment in `tests/` if you keep one (prefer zero).
6. `git grep -n "STRUCTURED_OUTPUT_CAPTURE\|completion-envelope.schema" -- bin delegation extensions tests skills` — zero matches.
7. `tools/ratchet.sh check` — report verbatim result.

## Completion envelope

Write your envelope to `$QQ_DISPATCH_RUN_DIR/ENVELOPE.md` following `delegation/manifests/ENVELOPE.md` (this dispatch predates the new contract; your run dir is confinement-provided — use it). Also end your final message with the word `envelope-written` and the absolute run-dir path. Anything contradicting the tree: stop, openQuestions.
