# Review brief — T-184: delete delegate confinement, one run dir, six-step delivery

You are the fresh-context reviewer for one large Change. Derive your verdict from the Change and the facts below; the author's conclusions are not evidence. Read the root `REVIEW.md` first — it supplies the owned review rules.

## Surface

- Repository: `/home/qqp/.local/share/qq/worktrees/qq/t-184-subtractive-delegation` (linked worktree of qq, common dir `/home/qqp/projects/qq/.git`)
- Base: `origin/main` (currently `d7263d2`, the T-185 merge); head: branch `feat/t-184-subtractive-delegation`
- Review the complete branch delta: `git diff origin/main..HEAD` (56 files, +4966/−2899, 22 commits) plus working-tree state (expected: only untracked `.brief-t184-*.md` owner files).

## Intent and acceptance (reconciled)

Task T-184 at `backlog/tasks/t-184 - …` — nine acceptance criteria bind. Approved plan `backlog/docs/plans/doc-122 - …` (Cuts 1–10 + keeps + T-177 reversal). In one paragraph: delegate confinement (Landlock/Landstrip) and the writer role are deleted and the git worktree is the only delegate boundary; the structured-output pipeline is replaced by run-dir `ENVELOPE.md`; one owner-created run dir per delegate holds BRIEF.md/ENVELOPE.md/TERMINAL; deliver-change is six steps with Task finalization before PR and two-state vocabulary; REVIEW.md's counter mandate is deleted; Observer intake is relaxed to named-branch-checkout verification; observer evidence is one-entry-per-object with finalize as sole analysis.json writer and explicit lineage as the only session-membership source; architect/disposition ceremony is cut (durable confirm, no exact-phrase rituals, no attempt-receipt command); keeps: `bin/qq-backlog decision update --content` + atomic doc supersede, intercom single-flight via install-time patch, bash unknown-arg refusal, role startup tool validation (extension-aware), `bin/qq-pi-inventory` replacing the README display parser, retire refusing until the guided observer package exists, derived-store materialization prose. T-177's global confined-delegation posture is reversed (decision-23/24/25 minted and cited in the ledger).

## Boundary and non-goals

OUT OF SCOPE by disposition: `openwiki/**` (derived), `backlog/**` beyond the T-184 records/decision-23/24/25/plan doc-122 (owner surface), `~/.pi` live state (activation), pi-subagents vendor fork, Herdr/cockpit/OpenWiki redesign, new confinement or fencing of any kind. Operator venue dispositions (2026-07-28): backlog verbs land as the `bin/qq-backlog` wrapper (upstream PR later); intercom single-flight lands as an install-time patch (`patches/pi-intercom/0.6.0/` + `bin/qq-patch-apply`), never a fork.

## Threat model

Engine (bash/python/TS) + prose Change. Trust boundaries: (1) `bin/qq-dispatch` is the delegation gate — it must still refuse non-Git/cross-Repository dispatch and invalid owner-created run dirs fail-closed, and its startup tool inventory must refuse unavailable tools including extension-pair violations; (2) `bin/qq-observe` intake/observer validation — relaxed but not weaker on topology, identity, or evidence checks that remain; (3) `bin/qq-change retire` — new refusal must not break existing rails or the absent-session posture; (4) `bin/qq-backlog` — surgical managed-markdown writes only, all-or-nothing rollback; (5) `bin/qq-patch-apply` — hash-verified, refuses drift, never forces. Declined classes: style/taste beyond REVIEW.md; the pre-existing confinement machinery being deleted (review the deletion's completeness, not the old design); backlog/openwiki historical references.

## Unenforced rules

The align/deliver-change/delegate-batch contracts are conduct prose — no tool enforces them. The ratchet enforces prose_words (now 8039) and zero-token budgets.

## Sources and facts (owner-verified; re-verify anything load-bearing)

- Full native suite green: every `tests/test-*.sh` passes; `tools/ratchet.sh check` clean; shellcheck clean on edited shell; `bin/qq-patch-apply check` reports pi-intercom `applied` on the real install.
- Known reviewed-during-integration fixes: researcher Context7 tools restored (decision-15) with extension-aware startup validation; `.github/workflows/ci.yml` Landstrip steps removed; three owner prose amendments (CONCEPTS backlog-wrapper invariant, delegate-batch materialization line, README inventory replacement).
- The implementer of Ticket A made one out-of-brief tools substitution (caught and reverted by the owner); everything else matched briefs.
- pi-lens autoformat was deleted operator-side via `~/.pi-lens/config.json format.enabled=false` (plan-sanctioned "or deleted"); not part of this diff.

## Permissions

Read-only: do not modify the Repository, do not push, do not edit `backlog/`. Scratch beneath your `$TMPDIR` only.

## Required finding evidence

Material introduced failures only: failure, file, line, concrete path, evidence. Review moves/deletions by invariant: kept behavior (git/cwd refusals, trusted-manifest validation, auth staging, session-root hygiene, timeout, process-tree cleanup, telemetry, T-177 global-canonical matrix) must survive semantically. A context gap reports the missing fact and why it controls the verdict; it is neither finding nor pass.

## Deliverable

Verdict (PASS or findings) with per-finding evidence; note any intent/boundary conflict against the Task, plan, and venue dispositions. Write your envelope to `$QQ_DISPATCH_RUN_DIR/ENVELOPE.md` and end your final message with `envelope-written` plus the absolute run-dir path.
