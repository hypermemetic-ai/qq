# QQ working rules

QQ is being rebuilt from a clean starting point.

- Add only pieces the operator explicitly selects.
- Prefer the smallest coherent implementation.
- Do not import legacy code, structure, policy, or automation by default.
- If preserving an old behavior requires costly repair, migration, or compatibility work, cut the behavior instead.
- Keep dependencies and machinery absent until a concrete need justifies them.
