# hypercore

Cleared to a clean slate on 2026-07-06 for a new architecture (to be
specified). The previous "operator-surface" design is preserved in full at
git branch `archive/v1-operator-surface` (tag `v1-archive`).

Nothing here is load-bearing yet — the rebuild starts from this point.
