---
id: doc-117
title: T-165.1 final fresh fix-delta review
type: other
created_date: '2026-07-27 04:48'
updated_date: '2026-07-27 04:48'
---
# T-165.1 final fresh fix-delta review

Run: `aafd10f6-cbeb-4530-b6f8-6aaabcdffe11`
Base: `6eca0ad`
Reviewed worktree: `/home/qqp/.herdr/worktrees/qq/operator-alignment-core`
Review date: 2026-07-27

VERDICT: FINDINGS

1. [P1] Broker-authoritative evidence capabilities remain forgeable and mutable through the model-writable channel — extensions/lib/qq-alignment-broker.ts:108-110, 227-239, 348-354, 425-444; bin/lib/qq-render-landstrip-policy.mjs:151-157. The broker places its channel beneath QQ_DISPATCH_RUNTIME_ROOT, while the outer orchestrator policy grants that entire runtime root writable. The orchestrator has bash/write, so it can directly create or replace evidence/<cap>.json. acceptProjection and openEvidence then trust schema-valid metadata from that writable file; openEvidence does not compare it with the broker's copied session-state metadata. I reproduced a direct channel write containing a model-selected canonical path/digest/inode, followed by acceptProjection and openEvidence returning those bytes. This violates the declared boundary that model packets/paths/provenance are untrusted and capabilities require broker authority. CONVERGENCE-BREAKER: channel confinement/capability authority has survived the two prior fix rounds.

2. [P1] Normal shutdown never proves the installed async run terminal before sealing/cleanup — extensions/lib/qq-alignment-broker.ts:494-516, 520-546, 550-562. stopOrchestrator reads status.details.state/status.details.status.state, but pinned pi-subagents RPC serializes the status tool as {text, details}; its installed status implementation returns terminal state only in text and details is {mode, results} (pinned vendor src/extension/rpc.ts:141-146 and src/runs/background/run-status.ts:397-405). Therefore a successful stop/status sequence times out into recovery-recorded; seal accepts that lifecycle, and later shutdown deletes the channel without terminal proof. I reproduced the installed response shape and observed recovery-recorded. The focused test masks this with an invented details.state fixture. CONVERGENCE-BREAKER: process/session-replacement lifecycle proof has survived the two prior fix rounds.

3. [P1] The orchestrator write policy exceeds the approved Repository Change-worktree root — bin/lib/qq-render-landstrip-policy.mjs:48-60, 151-157; README.md:475-479. The renderer adds every Git-registered linked worktree plus the invocation worktree independently of changeWorktreeRoot. A normal linked checkout registered anywhere else (for example /tmp/outside-linked) is consequently writable even though the approved boundary permits only ~/.herdr/worktrees/<repo>, non-main Git state, and private runtime. I reproduced a policy whose allowWrite contained an external registered checkout outside the supplied Change root. The README's new broader claim is not supported by T-165.1/doc-115 and cannot supply the missing disposition. CONVERGENCE-BREAKER: outer-root confinement has survived the two prior fix rounds.

Prose reductions otherwise preserve the reviewed qq obligations. Net production LOC and lexical decision-point delta are not reported: the tree is uncommitted (no per-fix commits), and no Repository-owned production-file classifier or lexical decision-point formula was found; inventing either would blend/guess the required counters.


## Structured checks

- `pass`: tests/test-qq-alignment-core.sh
- `pass`: tests/test-qq-root-profiles.sh
- `pass`: tests/test-qq-dispatch.sh
- `pass`: bash tests/test-qq-subagent-env.sh
- `pass`: bash tests/test-qq-extension-mount.sh
- `pass`: git diff --check 6eca0ad and jq parse of changed manifests
- `fail`: Installed RPC terminal-status-shape reproduction
- `fail`: Direct-channel forged evidence-capability reproduction
- `fail`: External registered-worktree allowWrite reproduction
- `inconclusive-under-substrate`: tests/test-qq-orchestrator-landstrip.sh
- `inconclusive-under-substrate`: tests/test-qq-pi-runtime.sh
- `inconclusive-under-substrate`: tools/ratchet.sh check

## Unresolved risks

- The real nested Landstrip probe remains CI/outside-substrate evidence only because this harness cannot nest seccomp.
- Live provider/root-interface UAT remains later as declared.
- The Pi runtime suite's one wrapper test could not create a temporary directory under the reviewer Landstrip substrate; owner evidence reports the suite passing outside this substrate. Ratchet likewise hit the documented /dev/fd confinement limit.
