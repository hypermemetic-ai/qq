---
id: doc-104
title: Plan — complete six-role execution profiles and assign Observer to Kimi K3
type: specification
created_date: '2026-07-27 04:25'
updated_date: '2026-07-27 04:26'
tags:
  - plans
  - execution-profile
  - models
---
# Plan — complete six-role execution profiles and assign Observer to Kimi K3

**Owning Task:** T-170  
**Status:** Approved by the operator in the project-home accountable session on 2026-07-27 (“Finish router first”; “Either way, I believe you can proceed.”).  
**Inherited contract:** T-152, doc-87, doc-88, decision-13, and doc-91.

## Intended outcome

Finish the already-approved second half of qq's role-routing system. One operator-only install-wide document becomes the sole compute authority for the six canonical roles. The active patched Pi seam resolves that complete profile before every logical request and pins it through its tool loop and auxiliary calls. Existing role authority, prompts, tools, access, network policy, independence, and lifecycle do not change.

The operator-settled role map assigns K3 only to Observer and GPT-5.6 Sol to every other role:

| Role | Provider/model | Effort | Service class |
|---|---|---|---|
| orchestrator | `openai-codex/gpt-5.6-sol` | `xhigh` | default/standard |
| architect | `openai-codex/gpt-5.6-sol` | `xhigh` | default/standard |
| implementer | `openai-codex/gpt-5.6-sol` | `xhigh` | default/standard |
| reviewer | `openai-codex/gpt-5.6-sol` | `xhigh` | default/standard |
| researcher | `openai-codex/gpt-5.6-sol` | `xhigh` | default/standard |
| observer | `kimi-coding/k3` | `max` | default |

A cited research report attached to T-170 records why Observer is K3's strongest relative quality fit. Speed and cost do not decide the assignment.

## Implementation

1. Verify the model-role quality conclusion from current primary and independent evidence and attach one confidence-tagged research report to T-170 before changing routing policy.
2. Add the smallest install-wide qq resolver around the landed patched-Pi execution-profile seam. It reads one atomically replaced operator-owned document containing exactly the six complete typed profiles and rejects malformed, missing, extra, unsupported, conflicting, or stale state before authentication or provider work.
3. Resolve immutable occupancy exactly as approved: a verified canonical delegated manifest source supplies the child-role assertion; the dedicated architect launcher supplies a launcher-bound architect assertion; every other non-child root is orchestrator. Reject missing, forged, conflicting, unknown, or untrusted delegated/architect assertions.
4. Admit canonical routes only from verified built-in provider/model provenance. Lock canonical pi-subagents seats against project definitions, manifest/invocation/default model selection, model fallback, and resume-time substitution. Remove delegated manifest compute pins only after the central authority is proven active.
5. Feed effective model, effort, requested service class, and provider acknowledgement into the existing footer and delegated run/status evidence. Add no new UI. Retire no existing service-class behavior until equivalent transport and accounting are proven.
6. Add exact schema, occupancy, authority, zero-call, hot-reload, tool-loop, auxiliary-call, transport, accounting, display, and rollback regressions. Run applicable Repository and patched-runtime Checks and fresh-context review.
7. Activate the complete map on the operator machine through the Repository-owned procedure, verify the patched identity and all six role paths, and keep rollback fail-closed. No credential or paid-usage change is authorized.

## Ownership boundary and non-goals

qq owns role names, occupancy, the one profile document, trusted canonical provider/model provenance, delegated-seat compute lock, and display integration. Patched Pi continues to own generic request atomicity, capability validation, transport, acknowledgement, and accounting.

No role expansion; authority/tool/prompt/access/network/Landstrip/lifecycle change; fallback; per-Repository or per-session override; provider credential mutation; service-tier experiment; speed/cost optimization; new UI; or silent migration belongs to this Change.

## Success evidence

- Fake providers observe zero calls for every invalid profile, occupancy, provenance, auth, or conflict case.
- Valid→valid changes apply to the next request; valid→invalid blocks; invalid→valid recovers; mid-request edits cannot affect tool continuations or nested work.
- Every root and delegated occupancy path resolves exactly one expected profile; same-name project agents and forged assertions fail.
- Canonical manifests and invocation parameters no longer control compute, and no fallback can substitute another model.
- Existing footer and run/status evidence agree with selected and acknowledged profiles.
- The activated machine reports the patched Pi identity and proves the six-role matrix: `observer = kimi-coding/k3:max`; all other roles use `openai-codex/gpt-5.6-sol:xhigh`.
- Applicable Repository and runtime suites pass and fresh review finds no material failure.

## Decision ledger

- Full six-role semantics and routing contract — T-152 and operator-approved doc-87.
- Upstream-gap evidence and bounded integration brief — doc-88.
- qq ownership, two-Change separation, and provider-provenance boundary — decision-13 and operator-approved doc-91.
- Finish the missing second Change now and assign K3 to exactly Observer — operator asked-and-answered exchange, 2026-07-27, captured in T-170.
- Set all five other roles, including Orchestrator and Architect, to GPT-5.6 Sol — operator realignment during implementation, 2026-07-27.
