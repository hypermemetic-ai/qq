#!/usr/bin/env bash
set -euo pipefail

SCRIPT_PATH=$(realpath -e -- "${BASH_SOURCE[0]}")
ROOT=$(cd -- "$(dirname -- "$SCRIPT_PATH")/.." && pwd -P)
DASHBOARD_COOKIES_BIN="$ROOT/node_modules/@hypermemetic-ai/qq-dashboard/bin/qq-dashboard-cookies"

exec "$DASHBOARD_COOKIES_BIN" "$@"
