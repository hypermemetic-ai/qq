From a62c12876e5614fe076f7aa47e1a644f5e18b19b Mon Sep 17 00:00:00 2001
From: surlej <ggpcv3@use.startmail.com>
Date: Mon, 6 Jul 2026 18:54:24 -0500
Subject: [PATCH] qq-ac: orchestrate emits loop phase to
 .orchestrate/state.json

Add bin/qq-phase, the single writer for a cheap, token-free progress
source a status widget reads (phase/index/detail/status/gate_run_id).
Wire the orchestrate skill to stamp each phase boundary, note it at the
loop level in AGENTS.md, gitignore the state dir, and cover the new
script in the gate lint.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
---
 .gitignore                  |   3 ++
 .no-mistakes.yaml           |   2 +-
 AGENTS.md                   |   5 ++
 bin/qq-phase                | 103 ++++++++++++++++++++++++++++++++++++
 skills/orchestrate/SKILL.md |  10 ++++
 5 files changed, 122 insertions(+), 1 deletion(-)
 create mode 100755 bin/qq-phase

diff --git a/.gitignore b/.gitignore
index 2edc940..51cf586 100644
--- a/.gitignore
+++ b/.gitignore
@@ -3,3 +3,6 @@
 
 # herdr session scratch (logs / pids) — local runtime state, never tracked
 .ntm/
+
+# orchestrate loop progress — transient per-repo state a status widget reads
+.orchestrate/
diff --git a/.no-mistakes.yaml b/.no-mistakes.yaml
index 83bce67..d20d792 100644
--- a/.no-mistakes.yaml
+++ b/.no-mistakes.yaml
@@ -8,7 +8,7 @@
 # Ref: https://kunchenguid.github.io/no-mistakes/reference/repo-config/
 agent: claude
 commands:
-  lint: "shellcheck bin/qq-wip bin/qq-wip-snapshot.sh bin/qqac-activate.sh bin/install.sh"
+  lint: "shellcheck bin/qq-wip bin/qq-wip-snapshot.sh bin/qqac-activate.sh bin/install.sh bin/qq-phase"
   format: ""            # no formatter wired (shfmt optional, not required)
   test: ""              # no formal suite; the gate's agent validates behavior against --intent
 intent:
diff --git a/AGENTS.md b/AGENTS.md
index c8a9245..92aec39 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -73,6 +73,11 @@ Support, any time: `research` (delegated, cited investigation → `research/`);
 `handoff` (compact state for a fresh agent when context runs low); `writing-skills`
 (author or edit a skill, eval-first).
 
+**Progress is stamped.** The conductor records the current phase to
+`.orchestrate/state.json` via `qq-phase <Phase>` at each boundary — cheap,
+token-free, per-repo. A status widget reads it and merges the gate's own
+`no-mistakes axi status` steps, so loop position and pipeline position show as one.
+
 ## Git — how work lands
 - **Commit on green.** A commit is a claim: commit only what
   `verification-before-completion` just proved — one commit per verified task or
diff --git a/bin/qq-phase b/bin/qq-phase
new file mode 100755
index 0000000..b19f5aa
--- /dev/null
+++ b/bin/qq-phase
@@ -0,0 +1,103 @@
+#!/usr/bin/env bash
+# qq-phase — stamp the orchestrate loop's current phase to .orchestrate/state.json,
+# a cheap, token-free source of truth a status widget reads. One writer; the loop
+# calls it at each phase boundary. Never makes an LLM call — pure local file + a
+# fast local-daemon read for the gate run id.
+#
+#   qq-phase <Phase> [--detail T] [--task T] [--status S] [--gate]  advance to a phase
+#   qq-phase gate                    attach the active gate run id (no phase change)
+#   qq-phase status                  print the current state JSON (widget read API)
+#   qq-phase done                    mark the loop complete (status=done)
+#   qq-phase clear                   remove state (loop idle)
+#
+# Phases: Align(1) Plan(2) Build(3) Verify(4) Sign-off(5) Review(6) Compound(7);
+# Triage(0) is pre-loop. Unknown names stamp verbatim with a null index.
+set -euo pipefail
+
+git rev-parse --is-inside-work-tree >/dev/null 2>&1 || { echo "not a git repo"; exit 1; }
+root=$(git rev-parse --show-toplevel)
+dir="$root/.orchestrate"
+file="$dir/state.json"
+branch=$(git symbolic-ref --quiet --short HEAD 2>/dev/null || echo detached)
+
+cmd="${1:-status}"; shift || true
+
+case "$cmd" in
+  status) [ -f "$file" ] && cat "$file" || echo '{"phase":"idle"}'; exit 0 ;;
+  clear)  rm -f "$file"; echo "cleared $file"; exit 0 ;;
+esac
+
+detail=""; task=""; status=""; gate=0; phase=""
+if   [ "$cmd" = "gate" ]; then gate=1; phase="__keep__"
+elif [ "$cmd" = "done" ]; then phase="__keep__"; status="done"
+else phase="$cmd"; fi
+
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --detail) detail="${2:-}"; shift 2 ;;
+    --task)   task="${2:-}";   shift 2 ;;
+    --status) status="${2:-}"; shift 2 ;;
+    --gate)   gate=1; shift ;;
+    *) echo "unknown arg: $1"; exit 1 ;;
+  esac
+done
+
+gate_id=""
+if [ "$gate" = "1" ]; then
+  gate_id=$( { no-mistakes axi status 2>/dev/null || true; } | grep -oE '[0-9A-Z]{26}' | head -1 || true)
+fi
+
+mkdir -p "$dir"
+now=$(date +%s)
+
+PHASE="$phase" DETAIL="$detail" TASK="$task" STATUS="$status" GATE_ID="$gate_id" \
+BRANCH="$branch" NOW="$now" FILE="$file" python3 - <<'PY'
+import json, os, tempfile
+
+file = os.environ["FILE"]
+try:
+    with open(file) as f: st = json.load(f)
+except Exception:
+    st = {}
+
+names = ["Triage", "Align", "Plan", "Build", "Verify", "Sign-off", "Review", "Compound"]
+idx = {n.lower(): i for i, n in enumerate(names)}
+idx.update({"signoff": 5, "sign off": 5})
+canon = {n.lower(): n for n in names}
+
+phase = os.environ["PHASE"]
+if phase == "__keep__":
+    phase = st.get("phase", "idle")
+else:
+    phase = canon.get(phase.lower(), phase)
+
+# a fresh run (no prior state, or the last one finished) starts clean
+if not st.get("started_at") or st.get("status") == "done":
+    st = {"started_at": int(os.environ["NOW"])}
+
+# detail is phase-scoped: a phase change drops it unless this call re-supplies one
+prev_phase = st.get("phase")
+if phase != prev_phase and not os.environ.get("DETAIL"):
+    st.pop("detail", None)
+
+st["phase"] = phase
+st["index"] = idx.get(phase.lower())
+st["total"] = 7
+st["branch"] = os.environ["BRANCH"]
+st["updated_at"] = int(os.environ["NOW"])
+
+for key, env in (("detail", "DETAIL"), ("task", "TASK"),
+                 ("status", "STATUS"), ("gate_run_id", "GATE_ID")):
+    v = os.environ.get(env, "")
+    if v:
+        st[key] = v
+
+st.setdefault("status", "working")
+
+tmp = tempfile.NamedTemporaryFile("w", dir=os.path.dirname(file), delete=False)
+json.dump(st, tmp, indent=2)
+tmp.write("\n")
+tmp.close()
+os.replace(tmp.name, file)
+print(json.dumps(st))
+PY
diff --git a/skills/orchestrate/SKILL.md b/skills/orchestrate/SKILL.md
index 6078356..3be020f 100644
--- a/skills/orchestrate/SKILL.md
+++ b/skills/orchestrate/SKILL.md
@@ -37,6 +37,16 @@ Two rules make the separation real:
 
 ## The run
 
+**Stamp progress at every phase boundary.** As you enter each numbered phase
+below, run `qq-phase <PhaseName>` (`Align`→`Plan`→`Build`→`Verify`→`Sign-off`→
+`Review`→`Compound`). This writes `.orchestrate/state.json` — the cheap, token-free
+source of truth a status widget reads (merged with the gate's own
+`no-mistakes axi status` steps). It is the loop's only self-report; without it the
+widget shows `idle`. Refinements: on a Build hand-off add `--detail "handoff k/n"`;
+on Verify set `--status green` or `--status red`; when you push to the gate add
+`--gate` (attaches the run id). Run `qq-phase done` after Compound, or whenever you
+stop conducting. The escape hatch (step 0) does not stamp — it never enters the loop.
+
 ### 0 — Triage first
 Trivial + local + reversible (typo, rename, one-liner)? Do it here, run
 `verification-before-completion`, commit on green (land per the project's Git mode —
-- 
2.43.0

