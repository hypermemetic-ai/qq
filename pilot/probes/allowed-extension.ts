export default function allowedPilotExtension(): void {
  // Intentionally empty. Its absolute path is used to prove explicit child-only
  // extension selection without loading project or user extension catalogs.
}
