Check the change at the given ref against the outbound brief.
You own test quality on both looks. You may modify tests and commit test-only changes. Never modify or commit production code.
Reject bad tests, extra tests, bloat, and over-engineering. Don't invent importance the brief did not give.
