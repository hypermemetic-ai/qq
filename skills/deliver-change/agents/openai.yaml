interface:
  display_name: "Deliver Change"
  short_description: "Drive an authorized Change to operator merge"
  default_prompt: "Use $deliver-change to take this authorized repository modification through a green pull request and operator merge."
